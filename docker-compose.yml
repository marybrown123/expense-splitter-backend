services:
  db:
    image: postgres:16
    container_name: expense-splitter-db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - expense_splitter_pg:/var/lib/postgresql/data

volumes:
  expense_splitter_pg: